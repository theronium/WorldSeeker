# WorldSeeker 開発引継ぎメモ

このファイルは「次にこのプロジェクトを触るとき、何から読んでどう動かせばいいか」をまとめた引継ぎ資料です。設計思想・仕様の正本は [`docs/design.md`](design.md) にあるので、内容の矛盾があれば design.md を優先してください。このファイルはそこへの入り口・現在地の要約です。

## 1分でわかる現状

Godot 4.7 (GDScript) 製の放置系オートバトラー的探索ゲーム。`godot/` 以下がプロジェクト本体。**コアループは実際に動く状態**まで実装済みで、シミュレーションによる動作確認も繰り返し行っている（後述の「動作確認の方法」参照）。

コアループ: NPCを雇用 → 担当セクションに割り当て → 毎日自動で発見・ゲート突破・戦闘を試行 → 節目は掲示板に記録 → 月末（実時間25分/等速）に集計 → 資金を雇用/訓練/施設拡張に再配分。

現在のワールド内容は 9エリア・39セクション・194フロアのデモ規模（`godot/scripts/world_data.gd`）。設計目標の「数百ノード」にはまだ遠く、継続的な肉付けが前提。後発エリアほど規模と難度を緩やかに引き上げる方針（詳細はdesign.md 8.1節）。

## 動かし方

Godot 4.7 は winget 経由でインストール済み（PATHには通っていない）:

```
GODOT_EXE="/c/Users/thero/AppData/Local/Microsoft/WinGet/Packages/GodotEngine.GodotEngine_Microsoft.Winget.Source_8wekyb3d8bbwe/Godot_v4.7-stable_win64.exe"
"$GODOT_EXE" --path godot          # 通常起動(GUI)
"$GODOT_EXE" --headless --path godot --quit   # コンパイルチェックのみ(CIっぽい用途)
```

コードを変更したら、必ず上記の `--headless --quit` でエラーが出ないことを確認してから完了報告する運用にしている。

### Androidビルド(2026-09-19に追加、実機 moto g05 / Android 15 で起動・セーブ動作を確認済み)

- 設定: `godot/export_presets.cfg`(arm64のみ、パッケージ名`com.theronium.worldseeker`)、`godot/project.godot`の`.mobile`フィーチャータグ上書き(描画をgl_compatibility、ストレッチをcanvas_itemsに。**Windows版には影響しない**)、CIは`.github/workflows/android.yml`(`v*`タグのpushでAPKをビルドしGitHub Releasesに添付、手動実行ではArtifactのみ)。
- **Godot 4.7のAPKエクスポートにはJava SDK(JDK 17)とAndroid SDK(`platform-tools`のadb、`build-tools`のapksigner)が必須**。公式ドキュメントには「不要」とあるが誤解を招く記述で、実際には未設定だと`Cannot export project ... due to configuration errors`で失敗する。パスはエディタ設定(`export/android/java_sdk_path`と`android_sdk_path`)でしか渡せず、Windowsは`%APPDATA%\Godot\editor_settings-4.7.tres`、Linux CIは`~/.config/godot/editor_settings-4.7.tres`。
- 署名キーは環境変数`GODOT_ANDROID_KEYSTORE_RELEASE_PATH/_USER/_PASSWORD`で渡す。CIでは`ANDROID_KEYSTORE_BASE64`/`ANDROID_KEYSTORE_PASSWORD`/`ANDROID_KEY_ALIAS`のSecretsから復元し、未登録なら使い捨てキーで署名する(その場合のAPKは、本物のキーで署名したAPKへの**上書きインストールができず**、一度アンインストールが必要=セーブが消える)。keystoreはコミット禁止(`.gitignore`済み)。
- ローカルでビルドを試すには、`APPDATA`環境変数を別ディレクトリに差し替えてGodotを起動する(そこにエディタ設定・エクスポートテンプレート`export_templates/4.7.stable/android_*`・keystoreを置く)。ユーザーの本物のエディタ設定(`%APPDATA%\Godot\editor_settings-4.7.tres`)を汚さずにビルドできる。**`user://`もこの`APPDATA`差し替えで切り替わることを確認済み**(`--headless --quit`起動でスキーマDBが差し替え先の`app_userdata\WorldSeeker\`に作られ、実セーブは無変化)。`--user-data-dir`は効かなかったので、**実セーブに触れずにゲームを起動して検証したいときは`APPDATA`の差し替えを使う**のが確実(コンパイル確認も同様)。
- 実機確認: `adb`(platform-tools)で`adb install -r`、`adb logcat -s godot`。**「USBデバッグを許可」ダイアログは「常に許可」にチェックしないと、adbデーモンの再起動のたびに`unauthorized`に戻る**。Androidのセーブ先は`/data/data/com.theronium.worldseeker/files/`(`user://`)で、リリースAPKは`run-as`で覗けない。
- **タッチUI(2026-09-19)**: `main.gd`の`_touch_ui`(`OS.has_feature("mobile")`、またはデスクトップでユーザー引数`-- --touch-ui`)がtrueのときだけ、(1)共有Themeのボタン上下余白を6→16にして高さ約50pxにし(`TOUCH_BUTTON_MARGIN_V`)、プルダウンの項目・Tree・ItemListの行も広げる、(2)エリア選択を横スクロールのタブ列から「◀ [プルダウン] ▶」に置き換える(`_build_area_nav_touch`)、(3)会話ウィンドウを高さ270・「次へ」を高さ72にして、内容が溢れたら上へ伸ばす。デスクトップ版の見た目は変えていない。ボタンを高くすると固定サイズのモーダルが画面(論理648px)からはみ出すので、モーダルを足す・中身を増やすたびに実機で確認すること(セーブ/ロードは一覧の最小高を詰めて対処済み)。
- **戻るキー(2026-09-19対応済み)**: Godot既定ではAndroidの戻るキーがアプリを即終了させるため、`project.godot`で`config/quit_on_go_back=false`にし、`main.gd`の`_on_go_back_requested()`(`NOTIFICATION_WM_GO_BACK_REQUEST`)で処理している。内側から1つずつ閉じる(確認ダイアログ → NPC管理/パーティの詳細画面は一覧へ → 開いているポップアップ)。会話中は何もしない。何も開いていなければ「もう一度戻るキーを押すと終了します」を2秒表示し、その間にもう一度押されたときだけ(オートセーブして)終了する。実機で確認済み。
- **adbで実機を操作する時は、タップやキー入力を送る前に必ず前面のアプリがゲームか確認する**こと(`dumpsys activity activities | grep topResumedActivity`)。確認せずに送ると、ホーム画面の別アプリ(カレンダー等)を操作してしまう(実際にやってしまった)。戻るキーは上記のとおりゲーム側で処理されるようになったが、ポップアップを閉じるテストには「閉じる」ボタンのタップを使う方が安全。
- 未確認: Ubuntu上のCIそのもの(ローカルのWindowsで同じ手順が通ることまでは確認)、実際に指で触った時の操作感(スクロール・パンのドラッグ、ズーム)。

### ロジックの検証方法(実際にこのセッションで多用した手法)

UIを手でクリックする代わりに、`main.gd` の `_ready()` の最後に一時的に `_diag_simulate()` 呼び出しを足し、`TimeSystem._advance_day()` をループで直接呼んで日数を進め、`EventDialogue.advance()/.choose()` で会話を消化し、最後に `print()` で状態を確認する。確認後は必ず元のファイルに戻す(`cp scripts/main.gd /tmp/backup.gd` してから編集し、後で `cp` し直す)。実際のマウス操作でUIをテストするのは避けること — Godotウィンドウにフォーカスが無いと、合成クリックが別の無関係なウィンドウに誤って当たるリスクがある。画面を目で確認したいときは、ウィンドウをバックグラウンド起動して `PrintWindow`(`PW_RENDERFULLCONTENT`フラグ)でそのウィンドウだけをキャプチャする(全画面キャプチャは他アプリの個人情報が写り込む事故が過去にあったので厳禁)。

## アーキテクチャ概要

`godot/scripts/` に autoload シングルトンとして分割（読み込み順は `godot/project.godot` の `[autoload]` を参照。データを使う側は使われる側より後に置くのが安全）:

| ファイル | 役割 |
|---|---|
| `time_system.gd` | 日/月の進行、倍速(1x/10x/100x/1000x)、月末の一時停止 |
| `world_map.gd` | エリア>セクション>フロアの階層とノードグラフ、ゲート判定 |
| `items.gd` | アイテム定義とNPC所持品(最小限) |
| `world_data.gd` | ワールドの中身(コンテンツ定義)。UIから完全に独立 |
| `world_schema_db.gd` | `world_data.gd`の内容を内容ハッシュ版管理でSQLiteへスナップショット出力(バージョンごとに別ファイル)し、そこからWorldMapを再構築するインポーターも持つ |
| `npcs.gd` | 雇用NPCの名簿、スキル/経験値、所持品、状態(探索中/回復中) |
| `wild_npcs.gd` | 野良NPC。雇用NPCと同じ実データを探索するが、ゲートは突破できない |
| `recruitment.gd` | 募集→候補提示→雇用 |
| `economy.gd` | 資金プール、各種コスト計算式 |
| `board.gd` | 掲示板。全体フィード(節目のみ)とセクション別スレッド |
| `action_log.gd` | リプレイ/観戦用の行動ログ(掲示板と同じ粒度、構造化列で記録) |
| `combat.gd` | 戦闘解決(HP・戦闘力・事前設定方針) |
| `event_dialogue.gd` | 汎用の会話ウィンドウ進行(分岐・outcome) |
| `exploration.gd` | 自動探索の日次処理。上記のほぼ全部を束ねる中心的モジュール |
| `save_system.gd` | セーブ/ロード。`addons/godot-sqlite`(GDSQLite)経由でSQLiteに保存 |

UI(表示のみ、ロジックは持たない): `scenes/main.tscn` + `scripts/main.gd`。

autoloadではなく`class_name`で共有する定義専用モジュールも2つある: `skill_types.gd`(`SkillTypes`、スキル種別)、`name_generator.gd`(`NameGenerator`、血筋ごとの事前生成名前辞書)。

`godot/addons/godot-sqlite/` は[2shady4u/godot-sqlite](https://github.com/2shady4u/godot-sqlite)のGDExtension(MITライセンス)。リポジトリ肥大化を避けるため**Windows x86_64とAndroid arm64のバイナリのみ**を同梱しており(debug/release両方、`bin/libgdsqlite.windows.*.dll`と`bin/libgdsqlite.android.*.arm64.so`)、Mac/Linux/iOS/Android x86_64(エミュレータ用)は含めていない。別OSで動かす場合は[リリースページ](https://github.com/2shady4u/godot-sqlite/releases)から該当バイナリを追加すること。

## 実装済み

- 雇用フロー(募集コスト・候補の質による雇用コスト変動)
- 担当セクション割り当てと自動探索(発見/ゲート再判定を毎日並列実行)
- ゲート4種類すべて実装: スキル(知恵/鍵開け/破壊)・戦闘・血筋・アイテム所持
- 全スキルが「使うたびに経験値」で自然成長(資金がなくても詰まりきらない)
- 戦闘: HP管理、事前設定方針(アイテム使用/撤退)、敗北しても永久ロストなし
- イベント会話ウィンドウ(VN風、分岐対応、ただしゲート結果自体はスキル値で自動判定・会話は演出専用)
- 掲示板のスレッド化(セクション別スレッド＋節目だけの全体フィード)で野良NPCノイズを分離
- 時間システム(1ヶ月=25分@1x、1x/10x/100x/1000x倍速)
- スキル訓練・施設拡張UI
- ワールドコンテンツとUIの分離(`world_data.gd`)、セクション単位のグリッド自動レイアウト
- セーブ/ロード(`save_system.gd`、GDSQLite、**複数セーブスロット対応**): 資金・雇用NPC名簿(血筋/スキル経験値/所持品込み)・ワールド探索進捗(発見/突破状態)・掲示板ログ・行動ログを、スロットごとに分けた`user://saves/slot_<id>.sqlite`へ日次オートセーブ+ウィンドウクローズ時保存。**起動時オートロードは廃止済み(2026-09-14、後述)**。アクティブなスロットは`user://worldseeker_meta.cfg`で管理
- ワールドスキーマのDB出力・再構築(`world_schema_db.gd`): `world_data.gd`の内容(エリア/セクション/ノード/接続/ゲート/イベント台本)から正規化JSONのSHA-256を`schema_version`とし、`user://schemas/schema_<version_id>.sqlite`へバージョンごとに別ファイルで保存。`world_data.gd`が正本の位置づけは変わらず、DBは派生キャッシュ。逆方向の`import_into_worldmap(version_id)`もある
- **ワールドスキーマの複数バージョン管理**: 各セーブスロットは保存のたびに自分が実際に読み込んでいるバージョンを記録し(`meta.schema_version`)、起動時・スロット切替時は必ずそのスロット自身のバージョンで`import_into_worldmap()`し直す(新規プレイ開始は常に最新バージョン)。`world_data.gd`の内容が将来変わっても、既存スロットは自分が生成された時点のマップ形状を保ち続ける。`WorldMap.reset()`/`Items.reset()`を先に呼んでから再構築する必要がある点に注意(呼ばないと別バージョンの要素が残ってしまう不具合になる。実際に一度検出して修正した)
- リプレイ用行動ログ(`action_log.gd`): 掲示板と同じ粒度(発見/ゲート突破/マイルストーン)を、npc_id/node_id等の構造化列付きで追記専用のSQLiteテーブルに記録。ログビューアー方式(決定論的リプレイではない)。プレイ回ごとの`run_id`をセーブデータと一緒に保存/復元
- 行動ログビューアーUI・セーブスロット管理UI(`main.gd`): 「行動ログ」ボタン→NPCで絞り込める専用パネル、「セーブ」ボタン→スロット一覧・切替・削除・確認ダイアログ付き新規プレイ開始ボタンの専用パネル
- セーブスロットの削除(`SaveSystem.delete_slot()`): アクティブな(現在開いている)スロットは削除不可というガード付き。UIからも確認ダイアログ経由で削除できる
- **マップUIの刷新**(2026-09-12〜13): GraphEdit/GraphNode/GraphFrameはノードグラフ**編集**用ウィジェットで、ノード・フレームをドラッグで動かせてしまいプレイ中に意図せずレイアウトが崩れる問題があったため撤廃。素の`Control`(Panel/PanelContainer/Label、`mouse_filter = IGNORE`)による自前描画+`draw_line()`での接続線描画に置き換えた。ScrollContainerだけでは広いマップを動き回りづらいため、キャンバスの空き領域を左ドラッグしてパンできる機能を自前実装
- **サイドバーのポップアップパネル化**: 機能追加でボタンが増えて240px幅に収まらなくなったため、「雇用」「NPC管理」ボタンを新設し、募集/雇用・NPC名簿/担当割り当て/スキル訓練/施設拡張をそれぞれ専用パネルに集約(既存の行動ログ/セーブと同じ方式に統一)。4パネルは同時に開けず、開いている間は半透明の遮断レイヤーが背景全体(マップ・サイドバー含む)の操作を止める(`_open_modal`/`_close_modal`)
- **NPC管理パネルをNPC選択起点のフローに再設計**: 「場所を選ぶ→NPCを選ぶ」ではなく「NPCを選ぶ→右側に担当セクション割り当て/スキル訓練が出る」という順に変更。担当セクションの選択肢は数が増えて長大化したため、エリア→セクションの2階層`Tree`(折り畳み可)に変更。スキル訓練は単独のドロップダウンをやめ、NPC選択パネル内にスキルごとの行(名前・短い説明・現在Lv/コスト・訓練ボタン)として統合し、各スキルの用途(`SkillTypes.SKILL_DESCRIPTIONS`)を明示した
- **暦の年月日表示**: `TimeSystem`に`start_year`/`start_month`(デフォルト0年1月、将来のシナリオ機能が上書きできるフック)と`format_date()`を追加し、月末までのカウントダウンとは別にサイドバーへ常設表示。セーブ/ロードにも対応
- **掲示板のウィンドウ化とマップの色分け・ズーム**(2026-09-13): 常設の右カラムだった掲示板を他機能と同じポップアップウィンドウ(`board_panel`)に統合し、サイドバーのボタン直下には全体フィード直近10件だけの小さなプレビューを常設(`board_preview_log`)。モーダル背景(`modal_blocker`)の不透明度も上げ、背景が透けて見づらくならないようにした。マップは、雇用NPC自身がそのノードに到達済みかを示す`WorldMap.found_by_employed`を新設し(`found`/`passed`はこれまで通り世界共通のグローバル状態のまま)、マス目を「自身(明るい色+ボーダー)/誰か(通常色。野良NPCのみ発見)/未踏破(暗い色、名前を????と表示)」の3色に塗り分け、発見済みだが未突破のマスは半透明にした。セーブデータにも`found_by_employed`列を追加(旧セーブは`ALTER TABLE`で補完。旧セーブのvillage/forest_edgeが「誰か」色に戻ってしまわないよう、ロード時は起動時ブートストラップの値とOR合成する)。マップ操作にはマウスホイールでのズームも追加(カーソル位置を中心に拡大縮小、レイアウト定数を再計算してマップを再構築する方式)
- **倍速UIの見直しと月末一時停止まわりのバグ修正**(2026-09-13): 倍速の段階を`1x/2x/4x/8x/16x`から指数的な`1x/10x/100x/1000x`に変更し、UIも1ボタンの順送りクリック(`cycle_speed()`)から、全段階を横に並べたボタン列(`ButtonGroup`による排他選択、`TimeSystem.set_speed()`)へ変更した。この調査の過程で2つのバグを発見・修正済み: (1) 上記の日付スキップ課題と絡む形で「月末の自動一時停止を1フレームの間に素通りしてしまう」バグ(詳細は直前の既知課題の項を参照)。(2) `next_month_button`が`month_ended`シグナル(その場で月末になった瞬間)でしか表示されない作りだったため、**一時停止した状態のままセーブして閉じ、次回起動でそのままロードすると、ボタンが表示されず再開する手段が無いまま日付が完全に止まって見える**という不具合があった(「倍速を上げても日付が全く進まない」という形で表面化しうる)。`_refresh_all()`でロード直後の`TimeSystem.is_paused`にも`next_month_button.visible`を同期させて修正
- `Play.bat`: リポジトリ直下のダブルクリック起動用ランチャー。Godotパスは`%LOCALAPPDATA%`基準、プロジェクトパスはバッチファイル自身の場所基準で解決するため環境非依存
- **収入システム(ワーカー配置型)・NPC自動命名・マップ操作性の改善**(2026-09-13): 「資金が増えない」(=`Economy.earn()`がどこからも呼ばれておらず、design.md 6章で構想されていた収入経路が未実装のままだった)への対応として`exploration.gd`に実装。担当NPCが**現在いる**セクションの「累計突破フロア数×セクション倍率(エリアの登場順で自動算出、`WorldMap.section_multiplier()`)」を月末収入として支給し、セクション完全攻略時には一時金(初回攻略のみ追加ボーナス、`WorldMap.is_section_reward_claimed()`で二重支給を防止)を出す。NPCごとに`Npcs.PostClearBehavior`(留まる/次の未踏破セクションへ自動移動、`WorldMap.next_section_to_explore()`)を設定できる。NPCの名前は`候補N`のプレースホルダのままだったのを、`name_generator.gd`(血筋ごとに雰囲気を変えた事前生成の名前辞書。design.md 8.1の「オフライン生成」方針に合わせた)から抽選するよう修正。マップには担当NPCの頭文字アイコンをセクション枠に表示(クリックでNPC管理パネルを開く)、マップ上部にはエリア選択ボタンをフロート表示してクリックでそのエリアまでスクロールする機能を追加。セーブデータには`npcs.post_clear_behavior`列と`section_rewards`テーブルを追加(前者は既存テーブルへの`ALTER TABLE`、後者は新規テーブル)。シミュレーションで検証済み: 名前生成・月次収入・攻略報酬(初回ボーナス込み)・二重支給防止・完全攻略後の自動再配置(1体のNPCが訓練済みの状態で「小さな王国」の複数セクションを連続で攻略・自動移動していく様子まで確認)
- **上記収入システムの追加バグ修正・UI改善**(2026-09-13、同日中のフォローアップ): (1) `Economy.earn()`自体は動いていたが、`main.gd`の`_on_month_ended`/`_on_day_advanced`が`_refresh_funds()`を呼んでいなかったため、月次収入やセクション攻略の一時金が入っても資金表示(UIラベル)がその場では更新されず「資金が増えない」ように見えるバグがあった。両ハンドラに`_refresh_funds()`を追加して修正。(2) NPC管理パネルの担当セクション選択肢が、実際には到達不可能な(どこからも繋がっていない)セクションも含め全39セクションを表示していたため、選んで割り当てても何も進行せず「移動できない」ように見える問題があった。`WorldMap.is_section_reachable()`(既に到達済み、またはfrontier_for_sectionが空でない)で候補を絞り込み、到達不可能なセクション(および該当セクションが1つも無いエリアごと)は選択肢に出さないよう`_populate_section_tree()`を修正。(3) NPCの状態(探索中/回復中(あと何日)/未割当)とスキルレベルの一覧が、NPC管理パネルにもロースター一覧にも表示されておらず(特に戦闘敗北後の「回復中」はUIのどこにも出ておらず、なぜ動いていないのか分からなかった)、`_npc_status_text()`/`_npc_level_summary()`を追加してNPC詳細・ロースター一覧の両方に表示するようにした
- **雇用の無反応バグ・マップNPCアイコンの重複バグ修正**(2026-09-13、さらに同日中のフォローアップ): (1) 雇用上限到達時・候補未選択時・雇用失敗時に`_on_hire_pressed()`/`_on_recruit_pressed()`が何のメッセージも出さず黙って処理を無視していたため、「連続で2人目を雇えない」という形で気づかれた(実際には雇用上限や未選択が原因でも、プレイヤーには理由が全く分からなかった)。雇用パネルに`hire_status_label`を追加し、失敗理由(上限到達/未選択/資金不足)と成功時のNPC名を都度表示するようにした。(2) `_refresh_section_npc_icons()`が古いアイコンの削除に`queue_free()`のみを使っており、実際に破棄されるのはアイドルタイム(そのフレームの終わり)まで遅延することを見落としていた。倍速時は`TimeSystem._process()`のwhileループが1フレーム内で何日分も`_advance_day()`を呼ぶため、その間に`_refresh_map()`が連続で呼ばれると`get_children()`がまだ残っている古いアイコンを返し続け、配置転換のたびにアイコンが重複表示されていく不具合になっていた(「次の月へ行かないとNPCアイコンが移動しない」として報告された)。`remove_child()`で即座にツリーから外してから`queue_free()`するよう修正し、1日ごとに配置転換を繰り返すシミュレーションで重複が起きないことを確認済み
- **NPC一覧のカード型UIへの作り直し**(2026-09-13、さらに同日中のフォローアップ): NPC管理パネルのロースター一覧が、血筋・担当セクション・状態を1行の連結文字列に詰め込んでいたため、情報が増えるたびにItemListの行がはみ出す/読めなくなる問題があった(「文字だけでは表現に限界がある」との指摘)。ItemListをやめ、NPCごとに素のControlで組んだカード行(`_create_roster_row()`)に作り直した: 血筋で色分けした頭文字アイコン(`BLOODLINE_COLORS`、将来は画像に差し替え想定)・名前・担当セクション(サブタイトル)・HPバー(ProgressBar)・状態バッジ(探索中=緑/回復中=橙/待機中=灰、`STATUS_COLORS`)を別々の部品として並べる。行全体を`toggle_mode`付き`Button`にして`ButtonGroup`で排他選択させ、中の子要素は`mouse_filter = IGNORE`にしてクリックをButtonまで素通りさせている。あわせて、NPC詳細パネル側にも同時期に追加していた「スキル: 知恵Lv3/...」という1行要約(下のスキル訓練欄と内容が重複するうえ折り返して読みにくかった)を削除した
- **アプリ共通テーマの導入(押せる/選べるものと、ただの文字の視覚的な区別を強化)**(2026-09-13、さらに同日中のフォローアップ): それまでボタン・Tree・ItemListがGodotの既定テーマのまま(個々のControlに`add_theme_stylebox_override`を都度呼ぶ以外のスタイル指定なし)だったため、「押せる/選択できるもの」と「ただの文字」の見た目の差が乏しく、選択してもわずかにしか色が変わらないという指摘があった。`main.gd`の`_ready()`冒頭で`_build_theme()`を呼び、Button/OptionButton/Tree/ItemListの各状態(通常・ホバー・押下/選択中・無効)に彩度差のはっきりしたStyleBoxFlatを設定した`Theme`をルートのControl(`self.theme`)に1回だけ設定し、以降add_childする全パネル・全ボタンに伝播させる方式にした(個々のボタン呼び出し箇所を1つずつ直す必要がない)。押下中/選択中は彩度の高い青(`Color(0.14, 0.46, 0.86)`)にはっきり変わるようにしてある。あわせて、NPCロースターのカード行(Button)がHBoxContainerを行いっぱいに敷いていたせいで中身がボタン自体の背景色をほぼ覆い隠していた点も、MarginContainerで数pxの余白を持たせて選択中の縁取りが見えるように修正した
- **NPC切替時の現在値プレビュー・マップのエリア可視化・フロア詳細ポップアップ**(2026-09-13、さらに同日中のフォローアップ):
  1. NPC管理パネルでNPCを切り替えても、担当セクションのTree選択状態が前のNPCのまま残っていて紛らわしかった。`_select_current_section_in_tree()`を追加し、NPC切替のたびにそのNPCの現在の担当セクションをTree上で選択済みにするようにした(完全踏破後の設定ボタンは元々`_refresh_npc_detail()`で同期済み)。
  2. マップ上に「小さな王国」のようなエリア名がどこにも表示されておらず、セクション枠の集まりがどのエリアに属すのか分からない問題があった。所属セクション群の外接矩形を計算し、それを大きく囲む背景枠+エリア名の見出し(`_create_area_panel()`)をセクション枠より先に(=描画上は背後に)追加した。実装中に、マップ上部にフロートで重ねているエリア選択バーがちょうどこの見出しの真上に重なって隠してしまう問題も見つかり、`MAP_NAV_BAR_CLEARANCE`分の余白を初期オフセットと`_on_area_nav_pressed()`のスクロール先計算の両方に追加して修正した。
  3. マップ上でフロアの箱をダブルクリックすると、そのフロア単体の詳細(所属セクション・状態・ゲート条件を人間可読な文章にしたもの・突破報酬アイテム・`SaveSystem.query_action_log_for_node()`で絞り込んだそのフロア単体の行動ログ)をポップアップ表示するようにした(`_on_map_double_click()`が箱の`Rect2`との当たり判定でフロア→セクションの順にヒットテストし、フロアに当たらなければセクション枠の掲示板スレッドを開く)。ダブルクリック判定は、素の左クリック分岐より先にチェックしないとパン開始(`_map_panning=true`)に食われてしまう点に注意
- **NPC管理パネルの横幅拡張・ボタン文言短縮・配置予測機能・マップのフロア単位アイコン化とロック表示**(2026-09-13、実プレイのスクリーンショットフィードバックへの対応): (1) NPC管理パネルの横幅を760pxから1000pxに拡張(表示しきれなくなったため)。(2) 「選択セクションに割り当てる」→「割り当て」、「このセクションのログを見る」→「ログ」、「留まる(収入維持)」→「ループ」、「次のセクションへ自動移動」→「先へ進む」に短縮(元の文言はtooltip_textとして残す)。(3) 施設拡張ボタンはコスト額が桁上がりするたびにボタン自体の最小幅が伸びてleft_col全体を押し広げていたため、ボタン文言を「施設を拡張する」固定にし、「雇用上限 +2」「コスト: N」を別のLabel(`facility_info_label`)に分離。(4) 担当セクション選択に「予測」ボタンを追加し、今月の予測報酬・撤退リスクを計算表示する(`Exploration.forecast_section()`、design.md 6.1節参照)。(5) マップのNPCアイコンをセクション単位からフロア単位の表示に変更(`_current_node_for_npc()`、design.md 5.3節末尾参照)。(6) 未到達セクションの枠下に🔒アイコンを表示し、ダブルクリックで開通条件の詳細ウィンドウを開けるようにした(`_show_section_lock_detail()`)。(7) 「ささやきの森」の入口フロアが自分の踏破扱いにならないと報告されたバグを調査・修正: 野良NPCがゲート無しフロアを先に発見・突破すると`found=true`になった時点で雇用NPCの日次処理対象から永久に外れ、担当NPCが稼働していても「誰か」色から「自身」色へ二度と切り替わらなかった。担当NPC稼働中は既に突破済みだが自分の踏破扱いになっていないフロアを合流させる`Exploration._claim_wild_progress()`を追加して修正(design.md 5.3節参照)。すべて`--headless --quit`でのコンパイル確認に加え、実際に起動してのスクリーンショット確認・ロック済みセクションでのダブルクリック確認・実セーブデータ(Day541、`forest_path`が`passed=true`かつ`found_by_employed=false`の状態)を使った修正確認まで実施済み
- **起動直後の日付スキップ不具合への対策(2026-09-13、部分的)**: `main.gd`の`_ready()`(マップUIの構築)にかかった実時間が丸ごと起動後最初の`_process`フレームの`delta`に乗ってしまい(Godotはフレームをまたいでdeltaをクランプしない)、特にロードしたセーブの`speed_multiplier`が10x〜1000xだった場合はそれだけで軽く1日分(`SECONDS_PER_DAY`=50秒)を超えてしまい、起動直後に日付が勝手に進む不具合があった(design.md 3章参照)。`TimeSystem`に`_first_process_frame`フラグを追加し、起動後最初の`_process`フレームのdeltaは無条件に捨てる(2フレーム目以降は通常通り加算)ことで対策したが、**これだけでは不十分だったことが後に判明**: `--headless --quit`を同じセーブに対して繰り返し実行しただけで日付がドリフトした(詳細は直後の事故報告を参照)。根本原因は未特定のまま design.md 11章に再掲してある
- **【事故報告・2026-09-13】診断作業による実セーブデータの損壊と初期化**: 上記の日付スキップ調査、および「セーブが出来ない、追加も出来ない」という別のユーザー報告の調査のために、稼働中のセーブ(スロット1、Day 540台)に対して直接`main.gd`の`_ready()`から雇用/施設拡張/`SaveSystem.save_game()`/`create_new_slot()`などを呼ぶ診断コードを追加して実行した。ここで2つのミスが重なった: (1) `create_new_slot()`は内部で「離れる前のスロットを保存」するため、一見「新規スロットを作るだけ」のつもりの呼び出しが、診断で加えた変更(テスト雇用・施設拡張)をそのまま実セーブに書き込んでしまった。(2) それを取り消そうとした復旧診断が「一番IDが大きいNPCが今回追加したテスト用NPCのはず」という前提で削除対象を選んだ結果、実際には育成済みの実在NPC(タウリエル)を誤って削除してしまった(なぜテスト用NPCの方が先に消えていたのかは未解明。GDSQLiteで同一ファイルへ短時間に複数回接続した際の書き込み反映タイミングが疑わしいが未確認)。さらに(3) その復旧診断コードを`_ready()`から元に戻し忘れたまま次の作業に進んでしまい、以後**起動するたびに診断コード(末尾で`get_tree().quit()`)が実行されて即終了し、しかも起動のたびにNPCを1体削除・資金と施設レベルを固定値へ強制上書きし続ける**状態になっていた。ユーザー報告の「セーブが出来ない、追加も出来ない」はこの症状だった。対応として、(a) 診断コードを完全に除去し通常起動を復旧、(b) 損壊したセーブ(スロット1)はタウリエルの詳細データが復元不能なため、ユーザーの了承を得て`slot_1.sqlite`を削除し初期状態(資金300/雇用上限3/NPC0体)からやり直す形で決着させた。再発防止のルールは[[godot_install_path]]相当のメモに追記済み: 保存を伴いうる診断を実行する前に必ず実ファイル(`user://saves/slot_<id>.sqlite`)自体をバックアップする、可能な限り使い捨てスロットで検証する、診断コードは復元後に必ずdiffで元通りであることを確認してから次の作業に進む
- **手動セーブ・進行の複製(分岐用)機能の追加**(2026-09-13、上記の事故を踏まえたユーザー要望への対応): 「オートセーブしかなく、新規プレイを開始する時にしか(その内部処理のついでにしか)保存されない。分岐前のチェックポイントとして能動的にセーブする手段が無い」という指摘に対応。(1) セーブスロットパネルに「今すぐセーブする」ボタンを追加、`SaveSystem.save_game()`をその場で呼び「保存しました(日付)」とラベル表示する(`_on_manual_save_pressed()`)。(2) 「現在の進行を複製する(分岐用の新規スロットを作る)」ボタンを追加、`SaveSystem.duplicate_current_slot()`(新規追加)が現在のスロットを`save_game()`で最新化した上でファイルをそのまま新しい空きスロットIDへコピーする。`create_new_slot()`と違いワールドやNPCの再構築は行わず、アクティブスロットも複製後は元のまま(呼び出し元は同じスロットで続行し、複製先は後から「このスロットを開く」で辿れる)。両機能とも診断コード([[godot_install_path]]の安全ルールに従い、事前にmain.gdをバックアップし実行後即座にdiffで元に戻したことを確認)で、保存→複製→複製先の削除まで一通りエラー無く動くことを確認済み
- **時間進行をフレームdelta蓄積からタイムスタンプ計算方式へ全面書き直し、日付ドリフト不具合を根本修正**(2026-09-13): 上記2件(部分的な対策止まりだった日付スキップ、およびそれが引き金になった事故)を受けてのユーザーからの指摘「スピード時間を厳密に守ろうとするのではなく遅延を許容すべき」「フレームベースでなく計算ベースで確実に行うべき」に対応。`TimeSystem._process()`をGodotのフレーム`delta`に依存しない設計に書き直した: `Time.get_ticks_msec()`のタイムスタンプ差分(`_last_check_ms`)から経過時間を計算し、1回のチェックで進める日数は最大1日に制限(`while`から`if`へ)、超過分は`_accumulated`に残して次回以降で段階的に追い上げる。起動時は`main.gd`の`_ready()`の一番最後で`TimeSystem.mark_boot_complete()`を呼び、そこまでの起動処理時間を経過時間の計算対象から明示的に除外する(design.md 3章参照)。検証: `--headless --quit`を5回連続実行しても日付が一切進まないこと(以前はこれだけで日付がドリフトしていた)、および`_last_check_ms`を60秒前に人為的にずらして1回のチェックにつき1日しか進まず残りが正しく繰り越されることを確認済み。あわせて「オートセーブを切りたい」という要望にも対応: `SaveSystem.autosave_enabled`(セーブスロットパネルのチェックボックス、`worldseeker_meta.cfg`に永続化)を追加し、日次/終了時の自動保存だけを専用の`SaveSystem.autosave()`経路に切り出してこのフラグを見るようにした(手動セーブ・複製・新規プレイ・スロット切替/削除など明示的な操作は影響を受けない)
- **セーブスロットパネルのレイアウト調整**(2026-09-13): ボタンが増えてパネル下端からはみ出す(「下に半分消えてしまう」)報告への対応。「今すぐセーブする」「進行を複製する(分岐用)」を左列、「このスロットをロードする」(「開く」から改称)「選択したスロットを削除する」「新規プレイを開始する」を右列に配置する2カラムへ再構成し、パネル高さも余裕を持たせた
- **NPC管理パネルを「一覧ビュー/詳細ビュー」の2画面構成に再設計**(2026-09-13): 常時2カラム(ロースター一覧+担当/スキル操作)だった構成を、ポートレートカードの一覧のみを表示する一覧ビュー(既定表示、担当/スキルは非表示)と、カードクリックで切り替わる詳細ビューに分割した。ロースターのカードは横長の行から、正方形の画像(未実装のため血筋色+頭文字で代用、将来ここをNPC画像に差し替える想定)の下に名前と状態バッジを積む縦長のポートレート型に作り直した(`_create_roster_card()`、`GridContainer`で配置)。詳細ビューは`TabContainer`で[ステータス(HP/状態/担当の要約+スキル訓練)][担当(セクション割り当てTree+割り当て/ログ/予測+完全踏破後設定)]の2タブに分け(当初3タブ案だったが「ステータスとスキルは同じ画面で良い」との判断で2タブに集約)、常時詰め込んでいた旧UIより縦横それぞれを広く使えるようにした。マップのNPCアイコンをクリックした場合は一覧を経由せず直接詳細ビューを開く(`_open_npc_panel(show_detail: bool)`に統一)。検証: 実際に稼働中のセーブ(Day94〜133、NPC2体在籍)をロードした状態でテストNPCを一時的に追加して一覧ビュー・詳細ビュー(担当タブ)双方をスクリーンショットで確認し、保存を一切呼ばずに`Stop-Process -Force`で終了して実セーブへの影響が無いことを確認した上で診断コードを削除
- **ロースターカードの横幅はみ出し修正**(2026-09-13、実プレイのスクリーンショット報告への対応): 上記カードの幅を「ポートレート96px+当て推量24px」で決め打ちしていたため、実在NPC「アーチボルド」のような長めの名前で名前+状態バッジの行がカード右端を越えてはみ出していた。原因は`Button`が(`VBoxContainer`等の`Container`と違い)子要素の必要サイズを自分の最小サイズに反映しない仕様で、当て推量の値が子要素の実際の必要幅より小さくても警告なく素通りしてしまうこと(design.md 4章、[[godot_install_path]]にGodotの一般的な落とし穴として記録済み)。カード幅を余裕を持った固定値(190px)へ広げ、名前ラベルに省略表示(`clip_text`+`OVERRUN_TRIM_ELLIPSIS`)とカード自体への`tooltip_text`(フルネーム確認用)を安全弁として追加した。検証は`--headless`起動+`await get_tree().process_frame`でレイアウト確定を待ってから各カードの子要素とカード自身の`get_global_rect()`を比較する診断で、実在の2 NPC名について右方向のはみ出し量が0であることを確認(1回目の検証後、診断コードを外そうとして誤って修正前のバックアップに戻してしまい修正ごと消してしまうミスがあったため、再度同じ修正を適用し直して再検証している)

- **パーティ制度・装備システム・ジョブ/固有スキルの実装**(2026-09-14): design.md 4.7/4.8/5.4/6.2節の設計をコードに反映。新設`parties.gd`(パーティ編成・担当セクション割り当て・回復。旧`npcs.gd`の`assigned_section`/`status`/`recovering_until_day`/`post_clear_behavior`をここへ移設)/`job_types.gd`(戦士/重戦士/盗賊/賢者/斥候の5職、得意スキルの経験値1.5倍、装備適性)/`equipment.gd`(材質6等級×武器5種/防具3カテゴリ、価格・戦闘力補正の定義)/`unique_skills.gd`(`NameGenerator`と同じ抽選パターンでジョブ別プールから固有スキルを1件付与。現状はジョブごとに1件のみ)。`combat.gd`は`resolve_party_encounter()`(並び順で1人ずつ戦い、撤退/戦闘不能になるたびに敵の残り戦闘力を次のメンバーへ引き継ぐ。全滅時はhp<=0になった者がいれば5日/全員自主撤退のみなら3日の回復)に置き換え、`exploration.gd`は担当単位をNPCからパーティに全面変更(スキルゲートはパーティ内最高値のメンバーが判定・経験値を得る、血筋/所持品ゲートはパーティ内の誰か1人が満たせばOK)。`main.gd`に「パーティ」「武器防具屋」パネルを新設し、NPC管理パネルにジョブ・固有スキル・装備・戦力の表示を追加、マップに推奨戦力表示とパーティ単位のアイコンを追加した。**旧セーブ(パーティ制導入前)からの移行**: `save_system.gd`の`load_game()`で`job`列が未設定(-1)のNPCを検出し、最高Lvスキルからジョブを推定して固有スキルを新規生成、`party_members`が空なら全NPCをid順に4人ずつグループ化して新規パーティを組む(担当セクションは各パーティとも空のまま。旧モデルは1NPC=1セクションだったため一意に継承できないための安全策)。実セーブのコピーで移行後の動作を確認済み。シミュレーション検証: 新規スロットでの初期パーティ生成(得意スキルが被らない4職・木等級装備)、300日規模の自動探索(発見・スキルゲート・血筋ゲートによる正しいブロック含む・月次収入)、`Combat.resolve_party_encounter()`の直接呼び出しによる強敵全滅/弱敵撃破/全員撤退の3パターン、セーブ/ロード往復。実装中に見つけた設計上の見落としとして、スキルLv同点時のタイブレークが常に並び順の先頭メンバーを優先してしまい(全員Lv0の序盤に他メンバーが自分の得意スキルで経験値を積む機会を得られない)、`_best_member_for_skill()`をジョブ適性優先に修正した
  - **【診断作業中の事故と対応】**: 検証用に実セーブ(slot_1)のコピー(slot_999)を作って移行テストを行う一方、別の小さめの検証(`_diag_ui_smoke_test`)で`SaveSystem.create_new_slot()`を呼ぶ際、直前に`current_slot_id`を退避し忘れたため、`create_new_slot()`内部の「離れる前のスロットを保存」処理がそのまま本番slot_1に書き込んでしまった(内容自体はfunds/NPC/日付を保った正しい移行結果で、データ損失は無かったが、本番へ触れない方針に反した)。診断直後に発覚し、事前に取得していたバックアップ(`user://`外の`WorldSeeker_backup_20260914/`)からslot_1.sqliteと`worldseeker_meta.cfg`を復元して元の状態に戻したことを確認済み。**教訓**: `SaveSystem.current_slot_id`を直接いじる診断では、`create_new_slot()`/`switch_to_slot()`など内部でその時点のスロットへ`save_game()`する関数を呼ぶ前に、必ず`current_slot_id`が安全な値(存在しない/使い捨てのID)になっていることを確認すること。[[godot_install_path]]の安全ルールに追記済み

- ~~既存194フロアの`enemy_power`再チューニング~~(2026-09-14実装済み): `combat.gd`の計算式を検証した結果、`enemy_power - combat_power >= 130`(最大HP100+USE_ITEM方針の回復+30)で即死する「壁」型の難度構造だと判明(それ未満ならUSE_ITEM方針で理論上ほぼ確実にいつか勝てるため、旧数値20〜120は装備込みパーティに対しほぼ壁として機能していなかった)。エリアごとに目標戦闘力を+60刻みで積み上げ、`enemy_power=目標+100`(安全マージン30)とする方式で36箇所を再設計。シミュレーションで、装備なしの初期パーティが最初のダンジョン2セクション目のボスで120日足止めされる(壁として機能)ことと、青銅装備だけでは力不足で足りない(意図通り、格下の敵で経験値を積んでから挑む設計)ことを確認。具体的な数値は初期チューニング値のまま
- ~~マップのエリア単位タブ化~~(2026-09-14実装済み。`_active_area_id`で表示中エリアを管理し、タブ切替のたびに`_seed_demo_world()`でそのエリア分だけレイアウトし直す。エリアをまたぐ接続は`_create_area_link()`で「→エリア名」リンクボタンに変換。`WorldMap.is_area_reachable()`新設で未到達エリアのタブに🔒表示。ウィンドウを起動してのスクリーンショットで表示を確認済み(実セーブ(slot_1)をロードした状態で目視確認、保存は一切発生させずStop-Process -Forceで終了))
- ~~固有スキル候補プールの拡充~~(2026-09-14実装済み。ジョブごと8種・計40種を`unique_skills.gd`に追加。新設した`combat_power_flat`/`max_hp_flat`効果種別も含め、100体抽選での分布確認と併せてシミュレーション検証済み)
- ~~転職(ジョブ変更)のプレイヤー向けUI・入手アイテム~~(2026-09-14実装済み): 消費アイテム「転職の秘薬」(`reclass_elixir`)を新設し、4〜9番目のエリアの手強い戦闘ゲート6箇所(既存の報酬チェーンに含まれない箇所)に`item_reward`として設置。NPC管理パネルの詳細ビューに転職先選択+「転職する」ボタンを追加(`Items.consume()`新設、所持時のみ押せる)。転職すると装備(新ジョブでは種類が合わなくなるため)は自動で外れるが、スキルLv・経験値・固有スキルは維持される。シミュレーションで、アイテム無しでは押せない/変化しない→付与後は押せて実際にジョブ・装備が変わることを確認済み
- **NPC肖像画像の反映**(2026-09-14): ユーザーが`sandbox/charactors/`に用意した生成済み画像40枚(`char_01.png`〜`char_40.png`、サイズはまちまち)を`godot/assets/portraits/`にコピーし、NPC管理パネルの「血筋色+頭文字」プレースホルダを実際の画像に差し替えた。新設`portrait_library.gd`(`class_name PortraitLibrary`)が`name_generator.gd`と同じ「血筋別プールからの抽選」パターンで雇用時に1枚割り当てる。40枚のうち3枚(骸骨/リッチ、ゴーレム、狼単体)は人型NPCとして不自然なため抽選対象から除外(ファイルは残置)、残り37枚を絵柄の雰囲気で4血筋に振り分けた(平民25/旧家の血筋4/森人の血5/王家の落胤3。振り分け根拠はdesign.md 4.2節参照)。ロースターカード・NPC詳細ビュー両方に反映(`_create_portrait_content()`を共通化、`TextureRect`の`STRETCH_KEEP_ASPECT_COVERED`でサイズまちまちの画像を96px正方形にクロップ表示)。旧セーブは次回ロード時に自動で肖像を新規抽選する(job移行と同じ方式)。実際にウィンドウを起動してのスクリーンショットで表示を確認済み(使い捨てスロットのみ使用、実セーブには触れず)
  - **【今回判明した安全確認上の注意点】**: 新しい永続化列(`portrait_id`)を追加すると、`_ensure_schema()`が次に実セーブを開いた瞬間(読み込みだけの操作でも)`ALTER TABLE`でその列を追加するため、事前バックアップとの生バイト`diff`が「差分あり」になる。今回はfunds/日付/NPC名など論理的な中身が完全一致することをPythonで確認し、実害の無いスキーマ追従だと判断してバックアップを再基準化した。[[godot_install_path]]にこの区別(生バイトdiffでなく論理内容で判定すること)を追記済み
- **雇用パネル・マップアイコンの肖像表示調整**(2026-09-14、実プレイのフィードバックへの対応): (1) 雇用パネルの候補者一覧を`ItemList`のテキスト行から、ロースターカードと同じ構造の肖像カード(`_create_candidate_card()`、64px、ロースターより一回り小さい縮小サイズ)に作り替えた。`Recruitment._generate_candidates()`が候補提示の時点で肖像を確定させておき(`Npcs.hire()`に`portrait_id`引数を追加)、実際に雇用されるNPCと一致させている。(2) マップのパーティ代表アイコンが20pxで小さすぎるという指摘に対応し、40px(2倍)に拡大。単純な重ね配置だと隣接ノードやテキストと衝突するため、`_create_node_box()`をHBoxContainer化してアイコン専用エリア(左側)+テキスト(右側)の横並び構成に作り替え、ノード枠の幅も248px(旧200px)に拡張してテキスト表示幅を実質維持した。実際にウィンドウを起動してのスクリーンショットで、アイコンがテキストと重ならず表示されることを確認済み(この時ユーザー自身がスロット2で新規プレイを試しており、初期パーティが「古い洞窟」のボス(推奨戦力180)で足止めされている実プレイ状況も確認できた=enemy_power再チューニングの壁が意図通り機能している実例)
  - **【この節で得た教訓】**: 診断中に自分の起動したのではないGodotプロセスがアクティブスロットを2に切り替えているのを発見し、一瞬「診断コードのバグでは」と疑ったが、`create_new_slot()`/`switch_to_slot()`の呼び出し元をgrepで全て洗い出したところ自分のコード内には該当箇所が無く、実際はユーザー自身が別途ゲームを起動して新規プレイを試していたことが判明した(`Get-Process | Where MainWindowTitle -like "*WorldSeeker*"`で確認)。ユーザーがこちらの作業と並行して実際にゲームを触っている可能性を前提に置き、ウィンドウが起動中は新規のスクリーンショット用ウィンドウを開かない、閉じているのを確認してから開く、という運用にした([[godot_install_path]]の既存ルールの実践)
- **撤退時のヘルプメッセージ**(2026-09-14): 戦闘ゲートで撤退・敗北した際、掲示板(該当セクションのスレッド)に「①武器防具屋で装備を強化する ②NPC管理パネルで戦闘力スキルを訓練する ③挑み続ければ経験値で自然に強くなる」という対策ヒントを投稿するようにした(`exploration.gd`の`_post_retreat_help()`、`_attempt_gate()`の戦闘ブランチから呼ぶ)。撤退後は`Parties.retreat_and_recover()`で数日再挑戦できないため、投稿頻度も自然と「回復サイクルごとに1回程度」に収まりスパムにならない。シミュレーションで、装備なし初期パーティが「ゴブリンキングの間」(敵戦闘力180)で敗北した際にヒント付きメッセージがセクションスレッドへ投稿されることを確認済み
  - **【診断中に判明した点】**: 検証の最中、ユーザーが別途ゲームを起動してさらに新規プレイ(スロット3)を試していたらしく、自分の診断コード(`create_new_slot()`を安全な使い捨てID経由で呼んだつもりが、`create_new_slot()`内部の`_next_free_slot_id()`はディスク上の実ファイルを基準に採番するため、結局スロット3を新規作成してしまっていた)と偶然重なった。診断後、自分が作った`slot_3.sqlite`/使い捨てID分のファイルを削除し、`worldseeker_meta.cfg`の`active_slot`をユーザーの直前の状態(2)に手動で戻した(このケースでは全体バックアップからの復元だと、それ以前にユーザー自身が進めていたスロット2への切り替えごと巻き戻ってしまうため、バックアップ復元ではなく該当キーだけを狙って直すべき場面だった)。**教訓**: `current_slot_id`を退避しても`create_new_slot()`自体は既存ファイルを基準に新しいスロット番号を採番することに注意(退避で守れるのは「離れる前のスロットの保存」だけ)。また、ユーザーが並行してセーブスロットを進めている状況では、`worldseeker_meta.cfg`を古い一括バックアップで丸ごと復元せず、直前に読み取った実際の値へピンポイントで戻すこと
- **起動時オートロードの廃止・新規プレイ時のスロット衝突バグ修正・スロット名前付け**(2026-09-14、ユーザー要望+バグ報告への対応): (1) 起動のたびに前回のアクティブスロットを自動ロードする仕様が、コード変更後の`--headless --quit`確認や診断作業のたびに実セーブへ意図せず触れるリスクの根本原因だった(上記の事故報告参照)ため廃止。`SaveSystem.current_slot_id`の初期値を`1`から`NO_ACTIVE_SLOT`(-1、「どのスロットファイルにも紐付いていない」を表す新設の定数)に変更し、`main.gd`の`_ready()`は`SaveSystem.load_game()`の代わりに新設の`SaveSystem.start_fresh_session()`(Economy/TimeSystem/Npcs/Parties等をresetし、初期パーティ付きの新規プレイをメモリ上にだけ用意する。ディスクには触れない)を呼ぶようにした。起動後は常に新規プレイから始まり、既存の進行を続けたい場合はセーブパネルから明示的に「このスロットをロードする」を選ぶ(design.md 8.2節)。(2) この変更に絡めて「新規プレイを開始する→セーブすると開始前の直近データを上書きしてしまう」というバグ報告を調査したところ、`create_new_slot()`の`_next_free_slot_id()`はディスク上の実ファイルのみを基準に採番するため、現在アクティブなスロットIDがまだ一度もファイル化されていない(=保存したことがない)場合、次の空きIDとしてそのアクティブID自身を返してしまい、「離れる前のスロットを保存」の一手で今離れようとしているデータを新規スロットのファイルとして書き込んだ直後、続く「新規状態を保存」の一手が同じファイルへ即座に上書きしてしまう(=直近データが一瞬で消える)ことが根本原因だと判明した(起動時オートロードが無くなったことで、この「未保存のままアクティブなスロット」状態が毎回の起動直後の通常状態になるため、修正前のままでは常時発生するバグになっていた)。`create_new_slot()`/`switch_to_slot()`双方で、離れる前の保存を「`current_slot_id != NO_ACTIVE_SLOT`の時だけ」行うよう修正し、`save_game()`自体にも「`current_slot_id`が`NO_ACTIVE_SLOT`ならその場で新しい空きスロットを割り当ててから保存する」という保険を追加(手動セーブ/オートセーブ/終了時保存のどの経路から呼ばれても安全に新規スロットへ行くようにするため)。(3) 「スロットに名前を付けられるように」という要望に対応し、各スロットのDBの`meta`テーブルに`slot_name`キーを追加、`SaveSystem.rename_slot(slot_id, name)`(アクティブでないスロットもDBを直接開いて名前だけ書き換えられる)を新設。セーブスロットパネルに名前入力欄+「選択中の名前を変更」ボタンを追加し、同じ入力欄を「新規プレイを開始する」時の名前付けにも流用した(`SaveSystem.create_new_slot(display_name)`)。全て`--headless --quit`でのコンパイル確認まで実施(実セーブへの書き込みが発生する診断コードは追加せず、ロジックの妥当性はコードレビューで確認)

- **説明用イベント会話3種の追加・撤退リスク予測の見落としバグ修正・パーティ解散UIの追加・初期雇用上限バグ修正**(2026-09-14、ユーザー要望+バグ報告への対応): (1) 新規プレイ開始時(NPCの配置/収入の仕組み/行ける場所の増やし方/進行速度)・初めての雇用直後(パーティは1人でも組める、という説明)・初めての戦闘撤退直後(「ループ」「先へ進む」は今まさに阻まれている状況には関係なく、セクション完全踏破後の挙動を決める設定だという区別)の3箇所に、一度きりの説明会話(`main.gd`のINTRO_TUTORIAL_SCRIPT/PARTY_TUTORIAL_SCRIPT、`exploration.gd`のRETREAT_TUTORIAL_SCRIPT)を追加した。「見た」フラグ(`SaveSystem.tutorial_intro_seen`/`tutorial_party_seen`/`tutorial_retreat_seen`)はスロットに紐付かず`worldseeker_meta.cfg`へ永続化するが、**再生を開始した時点ではなく、実際にプレイヤーが最後まで進めて`EventDialogue.finished`が発火した時点でONE_SHOT接続を使って書き込む**設計にしてある。理由は実際に一度踏んだ事故: 開始時点で即座に書き込む素朴な実装にしたところ、`--headless --quit`でのコンパイル確認(誰も会話を進めない)だけで実ファイルに「見た」が書き込まれてしまい、以後あらゆるコンパイル確認のたびに知らぬ間にチュートリアルが消費される状態になっていた(発覚後、汚れた`worldseeker_meta.cfg`は手動で復元した)。撤退時の会話は、フロア発見時の本編VN(`_on_node_found`)がまだ再生されていないタイミングで`_post_retreat_help()`が呼ばれることがあり、その場で`EventDialogue.play()`すると直後の本編VNの`play()`呼び出しに即座に上書きされてしまう(`EventDialogue.play()`は`is_active`を見ずに強制的に現在の会話を差し替えるため)。`call_deferred()`で1フレーム後に回し、`EventDialogue.finished`のたびに再度`call_deferred`で確認する、という「安全になるまで先送りし続ける」方式(`_retreat_tutorial_pending`/`_maybe_show_retreat_tutorial()`)で解決した。(2) `Exploration.forecast_section()`が「既に発見済みだが未突破のゲート」(野良NPCの先行発見や、前回このセクションを担当していた時に見つけたが突破できなかったもの)を予測のBFSから完全に見落としていたバグを修正した(`frontier_for_section()`が未発見ノードしか返さず、BFS自体も`neighbor["found"]`を弾く作りだったため)。「古い洞窟は予測では撤退リスク無しだったがゴブリンキングで撤退してしまった」という報告はこれが原因: ボスの間が既発見・未突破の状態で予測すると、そのセクションのフロンティアが空になり撤退リスクが常に0扱いになっていた。修正では、既発見・未突破のノードをeta=0として、未発見のフロンティア(eta=1/発見確率)より先にBFSキューへ積むことで、「先に積んだノードほどetaが小さい」というBFSの前提(=最初に見つかった撤退/敗北が最短の危険地点、という早期break最適化の根拠)を壊さないようにしている。(3) 以前から`parties.gd`に用意されていたが未接続だった`Parties.disband()`をパーティ詳細パネルから呼べるようにした(「パーティを解散する」ボタン+確認ダイアログ。装備/スキル経験値/固有スキルはNPC個体側が持つため解散しても失われない)。(4) 診断中に、無償の初期パーティが既に4人いるにもかかわらず`Economy.employ_cap`のデフォルトが3だったため、**新規プレイ開始直後から「雇用上限に達しています」と表示され、施設拡張(初期資金300を丸ごと使い切る額)をしない限り1人も追加雇用できない**という実質詰みの状態になっていることを発見・修正した(cap=3は4人パーティ制導入前の値が更新されないまま残っていたとみられる。5へ変更)。全て`--headless`起動+一時的な診断コード(`_diag_verify()`、確認後に完全に削除しdiffで元通りであることを確認済み)で、導入/雇用/撤退の3会話が正しいタイミングでのみ発火し二重発火しないこと・撤退会話が本編VNと衝突しないこと・パーティ解散でメンバーが未所属に戻ること・修正後のforecast_section()が実際に古い洞窟のボスをリスクとして検出することを確認した。診断中に`worldseeker_meta.cfg`へ書き込みが発生したため、実行のたびにバックアップと差分確認を行い、最終的に元の内容(`tutorial_*`キーなし)へ復元してある

- **マップのセクション名ボタン化・セクション起点のパーティ割り当て・未割当の視覚的強調・導入チュートリアルの前後編分割**(2026-09-14、実プレイのフィードバックへの対応): (1) マップのセクション名がただのLabel(`mouse_filter=IGNORE`)で「押せることが直感的に分からない」という指摘に対応し、`Button`化した(`_create_section_panel`)。暖色(アンバー)の専用スタイル(`_apply_section_title_button_style`)で通常ボタン(青系)と別系統にし、「📍」プレフィックスも付けた。到達不可能なセクションは`disabled=true`にし、個別に"disabled"styleboxを上書きしないことで共有Themeの薄暗いスタイルへ自然にフォールバックさせている(`section_title_buttons`辞書で保持し、`_refresh_map()`のたびに到達状況を追従させる。作成時点のまま放置すると、探索が進んで新たに到達可能になったセクションが次のエリアタブ切替/ズームまで押せないままになる不具合になるため)。(2) クリックすると新設の`section_assign_panel`(センタリングされたモーダル、node_detail_panelと同じパターン)が開き、そのセクションへ割り当てられるパーティの一覧(未割当のものを赤く強調して先頭に表示)から選んでその場で割り当てられるようにした(`_on_section_name_pressed`/`_refresh_section_assign_list`/`_create_section_assign_row`)。従来のパーティパネル(「パーティを選ぶ→セクションを選ぶ」)の逆順(「セクションを選ぶ→パーティを選ぶ」)を提供する形。実際の割り当て処理は`_assign_party_to_section()`に一本化し、パーティパネルの「割り当て」ボタン(`_on_assign_to_section_pressed`)側もこちらを呼ぶよう揃えた。(3) 「未割当のパーティが赤く光る」という要望に対応し、`_apply_unassigned_warning_style()`(共通ヘルパー、Button/PanelContainer両対応)をパーティ一覧のカードとセクション割り当てモーダルの行の両方に適用、サイドバーの「パーティ」ボタン自体も未割当のパーティが1つでもあれば赤字+「⚠未割当あり」表示に変える(`_refresh_party_roster()`)。「割り当て」ボタン自体も`_apply_primary_button_style()`(緑の主要アクション色)で目立たせた。(4) 「チュートリアルのマップ割り当てまでが分かりにくい、まずマップへの割り当てを行うよう誘導し、そこから本格的なチュートリアル会話を」という要望に対応し、導入会話(旧INTRO_TUTORIAL_SCRIPT)を前後編に分割した: 前編(`INTRO_TUTORIAL_PART1_SCRIPT`、起動時に再生)はマップでセクション名をクリックして割り当てる操作そのものへ誘導するだけに絞り、後編(`INTRO_TUTORIAL_PART2_SCRIPT`、収入/行ける場所を増やす方法/進行速度など、旧版の残り)は初めて実際にパーティ割り当てを行った直後(マップ経由/パーティパネル経由どちらでも)に再生する(`_maybe_show_assignment_followup_tutorial`/`_try_show_assignment_followup_tutorial`、`SaveSystem.tutorial_assignment_followup_seen`で一度きりに制御)。後編トリガーは撤退説明会話と同じ「安全になるまでcall_deferredで先送りし続ける」方式にしてあるが、パーティパネル/割り当てモーダルを開いたまま再生するとdialogue_panelが背後に隠れる(_open_modalの管理対象外のため)ため、再生前に`_close_any_modal()`で開いているモーダルを問わず閉じるようにした。全て`--headless`でのコンパイル確認まで実施。**この節での安全面の教訓**: 検証中にユーザーが並行して実際にゲームを起動してプレイしていることが判明し(`Get-Process`で確認、`worldseeker_meta.cfg`の`active_slot`が自分の関知しない値に変わっていたのがきっかけ)、その状態での`--headless --quit`が原因不明のexit code 1で落ちる事象が1回発生した。原因はおそらく、両プロセスが同じ`user://schemas/schema_<hash>.sqlite`(起動時に毎回開閉する共有ファイル)へほぼ同時にアクセスした際のファイルロック競合で、直後に同じ操作を再実行したところ問題なく成功したことからも一過性のものと判断した(`worldseeker_meta.cfg`の`active_slot`/`tutorial_intro_seen`の変化はユーザー自身の実プレイによるものであり、自分の診断が書き込んだものではないため復元しなかった)。`--user-data-dir`でのuser://分離を試みたが、このセットアップでは実際には分離されなかった(スキーマDBのパスが実際の`AppData`配下のままだった)。教訓として、ユーザーがゲームを起動中は新規のスクリーンショット用ウィンドウを開かないだけでなく、`--headless`診断自体も(同じ`user://`を共有し、確率は低いがファイルロック競合を起こしうるため)極力控える、少なくとも失敗時は即座にユーザーが並行プレイ中でないか確認してから原因を切り分けることをルール化した

- **イベント会話の確認用リセットボタン**(2026-09-14、「イベント確認が出来ないので設定にイベントフラグリセットを」という要望への対応): 一度見ると二度と出ない説明用イベント会話4種(導入前編/導入後編/初雇用/初撤退)の「見た」フラグを、`SaveSystem.reset_tutorial_flags()`でまとめて未視聴に戻せるようにした。セーブスロットパネル(design.md上「設定」に相当する唯一のパネル)に「イベント会話をリセットする(確認用)」ボタンを追加(`_on_reset_tutorials_pressed`)。導入前編は起動時にしかトリガーできないため、リセット直後にその場で再生する(スロットパネルを閉じてからでないと`dialogue_panel`が背後に隠れるのは他の会話と同じ)。残り3つは該当の操作(雇用/セクション割り当て/戦闘撤退)を実際に行うと再度表示される。セーブデータ自体(資金/NPC等)には触れない。`--headless --quit`でのコンパイル確認まで実施(ユーザーが並行してプレイ中の状態での確認だったが、1回目で問題なく成功し`worldseeker_meta.cfg`もユーザー自身の実プレイ分の変化以外は生じていないことを確認済み)

- **「ループ」設定を実際に周回する仕組みへ再設計**(2026-09-15、「ループでループしない。月初からいつでも一回分しか稼いでいないし、終わったらアイコンが最初に戻るべきだが戻っていない」というバグ報告への対応): 調査の結果、design.md上も「ループ=そのまま留まって収入源として維持する(月次の定額収入)」という仕様で、そもそも周回(ラップ)する概念自体が実装されていなかった(バグというより仕様と期待のズレ)。ユーザーに経済モデルの方向性を確認(「周回ごとに収入」を選択)した上で再設計した: 完全踏破済みセクションに配置されたパーティ(STAY/MOVE_ONを問わない。MOVE_ONで次の配置先が無く留まり続けているだけの場合も同様に扱う)は、月次収入の対象から外れる代わりに、1周(セクションのフロア数に応じた日数=フロア1つにつき1日で、最初のフロアから最後のフロアまで再度進む)ごとに月次収入と同じ単価(`LAP_INCOME_PER_POINT`)で収入を得る(`exploration.gd`の`_process_lap()`、`_on_day_advanced`から日次で呼ぶ)。月内に何周もできれば、その分だけ収入も増える。周回の起点日は`Parties.lap_start_day`(新設、SQLiteの`parties`テーブルに`lap_start_day`列を追加・`_ensure_column`で旧セーブにも補完、配置転換のたびに`assign_section()`がリセット)で管理し、マップ上のパーティアイコンも周回の進み具合に応じて最初のフロアから動かし、1周し終えるたびに最初のフロアへ戻す(`main.gd`の`_current_node_for_party()`、完全踏破後のフォールバックを「セクション最後のノード固定」から「lap_start_dayに応じた経過日数分だけノード一覧を進める」方式に変更)。月次収入(`_on_month_ended`)側は完全踏破済みセクションを対象から除外し、周回収入との二重取りを防いでいる。診断(old_cave_dungeon(4フロア)を人為的に完全踏破済みにし14日分シミュレーション)で、4日ごとに20資金(5×倍率1×4フロア)を得て(合計3周・60資金)、アイコンが4フロアを巡回してから最初のフロアへ戻ることを確認、セーブ/ロード往復でも`lap_start_day`が正しく保持されることを確認済み。**診断中の事故と対応**: 診断コードの`var party := Parties.get_parties()[0]`が`Array`の要素アクセス(型がVariantになる)に対し`:=`型推論を使ってしまい、パースエラーでmain.gd全体がロードに失敗し、プロセスがハングする事故があった(`Dictionary`型を明示する`var party: Dictionary = ...`に修正して解決。[[godot_install_path]]に新しい教訓として追記予定)。診断は`SaveSystem.save_game()`を経由したため新規スロット(slot_5)が作られたが、診断終了後に削除し`worldseeker_meta.cfg`もバックアップから復元した。なお診断が`list_slots()`経由で実スロット(1〜4)を開いた際、今回追加した`lap_start_day`列の`_ensure_column`マイグレーションが走り、該当ファイルの更新日時は変わったが、論理的な中身(資金/日付/NPC名など)は一切変化していないことをPythonで確認済み(想定通りのスキーマ追従。[[godot_install_path]]の既存の教訓通り)

- **武器防具屋: 現在の装備より下位の等級を買えてしまうバグの修正**(2026-09-15、「武器防具にメリットがないのにランク下のものを買えてしまう」というバグ報告への対応): `_create_shop_tier_button`が「装備中の等級と同じ」場合しか購入ボタンを無効化しておらず、下位の等級(equipment.gdの設計上、等級が上がるほど戦力・価格とも単調に増えるため、下位は戦力で確実に劣る割に安いだけで購入する理由が無い)を選んでも普通に購入・再装備できてしまっていた。`_create_shop_tier_button`の引数を`is_equipped: bool`から`current_tier: int`に変え、`tier < current_tier`(下位)の場合もボタンを無効化し、理由が分かるようtooltipも添えた(「現在の装備より下位のため、購入するメリットがありません」)。転職直後などで未装備(`current_tier == -1`)の場合はどの等級も下位に該当しないため制限は効かない。`--headless --quit`でのコンパイル確認まで実施

- **HP回復手段の追加(「回復」スキル・ホーム帰還時の全回復)・勝利時の個別撤退が療養をトリガーしないバグの修正**(2026-09-15、「復帰後HP回復手段がないため1のまま挑んでしまう、スキルで回復手段を実装。またホーム帰還時もフルに回復を」という要望への対応): 調査の結果、HPが回復する経路は`Parties.retreat_and_recover()`経由の療養明け(`Parties.is_available()`)しか無く、しかも`combat.gd`の`resolve_party_encounter()`は「パーティが敗北した場合」と「勝利したが誰かがhp<=0(戦闘不能)になった場合」しか療養をトリガーしておらず、**勝利はしたが個別にRETREAT方針で戦線離脱しただけ(hpが1付近まで減るがhp<=0にはならない)のケースが漏れていた**ため、そのメンバーが療養も無いままHP1付近で翌日以降も平然と次の戦闘に挑んでしまっていた(バグの直接原因)。`any_defeated`(hp<=0)だけを見ていた療養要否の判定を、RETREATによる個別離脱も含む`any_setback`に広げて修正した。加えて、要望通り新しいスキル「回復」(`SkillTypes.Skill.HEALING`)を追加し、パーティ内で最も「回復」が高いメンバーが毎日HP満タンでない仲間を`5 + 3×Lv`回復させるようにした(Lv0でも最低限は回復し無課金プレイでも詰まらない設計。実際に回復させた日だけ経験値が入る、`exploration.gd`の`_apply_daily_healing()`)。「ホーム帰還時もフルに回復を」については、パーティを担当セクションから外した未割当(IDLE)状態を「ホームにいる」ものとして扱い、毎日その場で全回復させる仕様を新設した(`_heal_idle_party()`)。以前はEXPLORING以外のパーティを日次処理から丸ごとskipしていたため、IDLE状態のパーティは一切回復しないまま放置されていた(これも今回セットで修正)。スキル追加に伴い、`Npcs.load_state()`が旧セーブ(HEALING追加前に保存され`npc_skills`テーブルに該当行が無い)のNPCについて欠けているスキルキーをLv0で補完するよう修正した(将来また新スキルを追加した時にも効く汎用的な対策。補完しないと`skill_level()`等のキーアクセスでエラーになるところだった)。診断で、(1)IDLE状態のパーティがHP20→100(全回復)、(2)EXPLORING中のパーティが「回復」Lv3のメンバーによりHP20→34(+14=5+3×3、期待通り)、(3)意図的に組んだ「勝利したが1人が個別撤退」シナリオで撤退したメンバーがHP1になり、かつパーティ全体がRECOVERING状態に遷移すること(修正前はEXPLORINGのまま=バグを再現し、修正後はRECOVERING=修正確認)、(4)HEALING行が無い模擬旧セーブデータを読み込んでもクラッシュせずLv0で補完されること、をそれぞれ確認済み。design.md 4.3節(スキル一覧)・5.4節(戦闘)も合わせて更新した。`--headless --quit`でのコンパイル確認・一時的な診断コード(確認後に完全に削除しdiffで元通りであることを確認済み)による検証を実施。診断はSaveSystem.save_game()等を一切経由しないため実セーブへの影響は無い

## 未着手・既知の課題(詳細は design.md 11章)

- **スキーマバージョンのガベージコレクション**: `user://schemas/`配下のバージョンファイルは増える一方で、参照されなくなった古いバージョンを消す仕組みはまだない(現段階では実害なし)
- **Android版の残課題**: (1)[解決済み 2026-09-19]行動ログなどで英数字の直後にある`「`が豆腐(□)になる問題は、端末の`/system/fonts/NotoSansCJK-Regular.ttc`をテーマの既定フォントのフォールバックに登録して解決した(`main.gd`の`_load_system_cjk_font()`、フォント同梱なし=アプリサイズ増加なし、実機で確認済み)。原因は字形が無いことではなく、Godotが「英字の並びの直後にあるCJK記号」を英字として扱いCJK非対応のシステムフォントを選ぶこと(端末のロケールはja_JPで、`language="ja"`やSystemFontの名前指定では直らなかった)。**新しい文字が出るたびのパッチは不要**(フォントは全ての日本語文字を持つため)。ファイルが無い端末では従来どおりシステム任せになる。ttc内の日本語版は先頭(face 0)で、`fonts.xml`のlang="ja"がindex=0、ko=1、zh-Hans=2、zh-Hant=3。`font_set_face_index`が実際に効くかは未確認、(2)プロジェクトアイコン未設定(エクスポート時にエラー行が出る。APKは生成される)、(3)ボタンの高さとエリア選択はタッチ対応済みだが、縦持ち対応・マップのパン/ズームの指操作(2本指ピンチ等)・左メニューの掲示板プレビューが画面下に切れる点は未対応、(4)[解決済み 2026-09-19]NPC管理の詳細画面が、ボタンが高くなった分、画面下にはみ出していた問題は、「一覧へ戻る」を固定し、それ以外をScrollContainerに入れて解決した(実機で最下段の転職欄までスクロールできることを確認)。同じ実機確認で、パーティ詳細・掲示板・割り当て・雇用(候補が並んだ状態)・NPC管理一覧・武器防具屋・行動ログ・セーブ/ロードの各パネルが画面内に収まることを確認済み。雇用の候補カードは、CJKフォールバックで行が高くなり「コスト」行が切れたため、カードの高さをツリーに入った後の実測値から決めるよう直した(素のButtonは子の必要サイズを含めない罠、[godot_install_path]参照)。フロア詳細パネルは未確認、(5)GitHubリポジトリ・署名キーのSecrets登録は未実施(戻るキー処理とダブルタップ(「ダブルタップで詳細」の文言・動作とも)は2026-09-19に実機で確認済み)
- **コンテンツのさらなる肉付け**(194フロア → 数百ノード目標)
- 経済・戦闘・野良NPC頻度・装備価格などの数値は初期チューニング値。実プレイでの調整が未実施
- マルチプレイ化(設計上は想定済みだが未着手。セッション型の時間進行を常時稼働サーバーに移行する必要がある)

## 次にやると良さそうなこと

優先度順というより「どれからでも進められる」独立した候補:
1. コンテンツ追加(`world_data.gd` に `add_area`/`add_section`/`add_node`/`set_event_scripts` を呼び足すだけ。後発エリアほど規模を拡大する方針は8.1節参照)
2. 実際にGodotエディタでしばらく触ってみて、体感バランス(コスト・確率・敵戦闘力の壁の高さ)を調整する(パーティ制度・装備・ジョブ・enemy_powerの再チューニング・マップタブ化・転職まで一通り実装しシミュレーションでは確認済みだが、あくまで一次確認止まりなので実プレイでの検証が必要)
